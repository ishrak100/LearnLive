# LearnLive Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         LEARNLIVE SYSTEM                             │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────────┐                      ┌──────────────────────┐
│   CLIENT 1           │                      │   CLIENT 2           │
│   (Teacher)          │                      │   (Student)          │
│                      │                      │                      │
│  ┌────────────────┐  │                      │  ┌────────────────┐  │
│  │  Login GUI     │  │                      │  │  Login GUI     │  │
│  └────────────────┘  │                      │  └────────────────┘  │
│  ┌────────────────┐  │                      │  ┌────────────────┐  │
│  │  Teacher       │  │                      │  │  Student       │  │
│  │  Dashboard     │  │                      │  │  Dashboard     │  │
│  └────────────────┘  │                      │  └────────────────┘  │
│  ┌────────────────┐  │                      │  ┌────────────────┐  │
│  │  TCP Client    │  │                      │  │  TCP Client    │  │
│  │  (client.py)   │  │                      │  │  (client.py)   │  │
│  └───────┬────────┘  │                      │  └───────┬────────┘  │
└──────────┼───────────┘                      └──────────┼───────────┘
           │                                             │
           │              TCP Connection                 │
           │              (JSON Protocol)                │
           └─────────────────┬───────────────────────────┘
                             │
                    ┌────────▼────────┐
                    │                 │
                    │   TCP SERVER    │
                    │  (server.py)    │
                    │                 │
                    │  - Port: 8888   │
                    │  - Multi-thread │
                    │  - 50 clients   │
                    │                 │
                    └────┬───────┬────┘
                         │       │
          ┌──────────────┘       └──────────────┐
          │                                     │
┌─────────▼─────────┐                  ┌────────▼────────┐
│                   │                  │                 │
│  DATABASE         │                  │  NOTIFICATION   │
│  (database.py)    │                  │  (notify.py)    │
│                   │                  │                 │
│  ┌─────────────┐  │                  │  ┌──────────┐   │
│  │  MongoDB    │  │                  │  │  Gmail   │   │
│  │             │  │                  │  │  SMTP    │   │
│  │  - users    │  │                  │  │          │   │
│  │  - classes  │  │                  │  └──────────┘   │
│  │  - assign.  │  │                  │                 │
│  │  - submiss. │  │                  └─────────────────┘
│  │  - announce │  │
│  │  - comments │  │         ┌────────────────────┐
│  │  - materials│  │         │   FILE HANDLER     │
│  └─────────────┘  │         │  (file_handler.py) │
│                   │         │                    │
└───────────────────┘         │  - Chunked upload  │
                              │  - Downloads       │
                              │  - Validation      │
                              └────────────────────┘
```

## Protocol Message Flow

### 1. Authentication Flow

```
Client                          Server
  │                               │
  ├───── LOGIN ─────────────────> │
  │     {email, password}         │
  │                               ├─ Verify credentials
  │                               ├─ Generate token
  │ <──── SUCCESS ────────────────┤
  │     {token, user_data}        │
  │                               │
```

### 2. Create Class Flow (Teacher)

```
Teacher Client                  Server                   Database
  │                               │                         │
  ├─── CREATE_CLASS ─────────────>│                         │
  │   {token, class_name,         │                         │
  │    description, subject}      │                         │
  │                               ├──── Insert ────────────>│
  │                               │                         ├─ Generate code
  │                               │                         ├─ Save class
  │                               │<──── class_doc ─────────┤
  │<──── SUCCESS ─────────────────┤                         │
  │   {class_id, class_code}      │                         │
  │                               ├──── NOTIFY ────────────>│
  │                               │  (Email notification)   │
  │                               │                         │
```

### 3. Submit Assignment Flow (Student)

```
Student Client                  Server                   Database
  │                               │                         │
  ├─ SUBMIT_ASSIGNMENT ──────────>│                         │
  │  {token, assignment_id,       │                         │
  │   submission_text}            │                         │
  │                               ├─── Verify student ─────>│
  │                               ├─── Save submission ────>│
  │<─── SUCCESS ──────────────────┤                         │
  │                               ├──── NOTIFY (Teacher) ───>│
  │                               │                         │
```

### 4. File Transfer Flow

```
Client                          Server
  │                               │
  ├─ START_FILE_TRANSFER ────────>│
  │  {file_name, file_size}       │
  │                               ├─ Validate
  │<─── ACK ──────────────────────┤
  │                               │
  ├─ FILE_CHUNK (1) ─────────────>│
  ├─ FILE_CHUNK (2) ─────────────>│
  ├─ FILE_CHUNK (3) ─────────────>│
  │        ...                    │
  ├─ END_FILE_TRANSFER ──────────>│
  │                               ├─ Save file
  │<─── SUCCESS ──────────────────┤
  │  {file_id, file_path}         │
  │                               │
```

## Multi-Threading Model

```
┌──────────────────────────────────────────────────────────┐
│                    SERVER PROCESS                         │
│                                                           │
│  ┌────────────────────────────────────────────────────┐  │
│  │              MAIN THREAD                           │  │
│  │                                                     │  │
│  │  while True:                                       │  │
│  │      client_socket, addr = server.accept()        │  │
│  │      thread = Thread(target=handle_client)        │  │
│  │      thread.start()                               │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  ┌───────────────┐  ┌───────────────┐  ┌──────────────┐ │
│  │ Client Thread │  │ Client Thread │  │ Client Thread│ │
│  │      #1       │  │      #2       │  │      #3      │ │
│  │               │  │               │  │              │ │
│  │  ┌─────────┐  │  │  ┌─────────┐  │  │  ┌────────┐  │ │
│  │  │ Handle  │  │  │  │ Handle  │  │  │  │ Handle │  │ │
│  │  │ Messages│  │  │  │ Messages│  │  │  │Messages│  │ │
│  │  └─────────┘  │  │  └─────────┘  │  │  └────────┘  │ │
│  │               │  │               │  │              │ │
│  │  ┌─────────┐  │  │  ┌─────────┐  │  │  ┌────────┐  │ │
│  │  │ Process │  │  │  │ Process │  │  │  │Process │  │ │
│  │  │ Requests│  │  │  │ Requests│  │  │  │Requests│  │ │
│  │  └─────────┘  │  │  └─────────┘  │  │  └────────┘  │ │
│  └───────────────┘  └───────────────┘  └──────────────┘ │
│                                                           │
│                    Shared Resources:                      │
│                    - Database connection                  │
│                    - Active clients dict                  │
│                    - File handler                         │
└──────────────────────────────────────────────────────────┘
```

## Database Schema

```
┌─────────────────────────────────────────────────────────┐
│                     MONGODB DATABASE                     │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────────────────────────────┐         │
│  │  users                                     │         │
│  ├────────────────────────────────────────────┤         │
│  │  _id: ObjectId                             │         │
│  │  name: String                              │         │
│  │  email: String (unique)                    │         │
│  │  password_hash: String                     │         │
│  │  role: String (teacher/student)            │         │
│  │  created_at: DateTime                      │         │
│  └────────────────────────────────────────────┘         │
│                                                          │
│  ┌────────────────────────────────────────────┐         │
│  │  classes                                   │         │
│  ├────────────────────────────────────────────┤         │
│  │  _id: ObjectId                             │         │
│  │  class_name: String                        │         │
│  │  description: String                       │         │
│  │  subject: String                           │         │
│  │  teacher_id: ObjectId → users             │         │
│  │  class_code: String (unique, 6-char)      │         │
│  │  student_ids: [ObjectId]                  │         │
│  │  created_at: DateTime                      │         │
│  └────────────────────────────────────────────┘         │
│                                                          │
│  ┌────────────────────────────────────────────┐         │
│  │  assignments                               │         │
│  ├────────────────────────────────────────────┤         │
│  │  _id: ObjectId                             │         │
│  │  class_id: ObjectId → classes             │         │
│  │  title: String                             │         │
│  │  description: String                       │         │
│  │  due_date: String                          │         │
│  │  max_points: Int                           │         │
│  │  created_at: DateTime                      │         │
│  └────────────────────────────────────────────┘         │
│                                                          │
│  ┌────────────────────────────────────────────┐         │
│  │  submissions                               │         │
│  ├────────────────────────────────────────────┤         │
│  │  _id: ObjectId                             │         │
│  │  assignment_id: ObjectId → assignments    │         │
│  │  student_id: ObjectId → users             │         │
│  │  submission_text: String                   │         │
│  │  file_path: String (optional)              │         │
│  │  submitted_at: DateTime                    │         │
│  └────────────────────────────────────────────┘         │
│                                                          │
│  ┌────────────────────────────────────────────┐         │
│  │  announcements                             │         │
│  ├────────────────────────────────────────────┤         │
│  │  _id: ObjectId                             │         │
│  │  class_id: ObjectId → classes             │         │
│  │  teacher_id: ObjectId → users             │         │
│  │  title: String                             │         │
│  │  content: String                           │         │
│  │  created_at: DateTime                      │         │
│  └────────────────────────────────────────────┘         │
│                                                          │
│  ┌────────────────────────────────────────────┐         │
│  │  comments                                  │         │
│  ├────────────────────────────────────────────┤         │
│  │  _id: ObjectId                             │         │
│  │  announcement_id: ObjectId → announce.    │         │
│  │  user_id: ObjectId → users                │         │
│  │  comment_text: String                      │         │
│  │  created_at: DateTime                      │         │
│  └────────────────────────────────────────────┘         │
│                                                          │
│  ┌────────────────────────────────────────────┐         │
│  │  materials                                 │         │
│  ├────────────────────────────────────────────┤         │
│  │  _id: ObjectId                             │         │
│  │  class_id: ObjectId → classes             │         │
│  │  material_name: String                     │         │
│  │  material_type: String                     │         │
│  │  file_path: String                         │         │
│  │  uploaded_at: DateTime                     │         │
│  └────────────────────────────────────────────┘         │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## Network Protocol Specification

### Message Format (JSON over TCP)

```json
{
  "type": "MESSAGE_TYPE",
  "token": "session_token",
  "data": {
    // Message-specific fields
  }
}
```

### Message Types (20+)

**Authentication:**

- `LOGIN` - User authentication
- `SIGNUP` - User registration

**Class Operations:**

- `CREATE_CLASS` - Create new class
- `JOIN_CLASS` - Join with code
- `VIEW_CLASSES` - Get user's classes
- `VIEW_STUDENTS` - Get enrolled students
- `REMOVE_STUDENT` - Remove student
- `DELETE_CLASS` - Delete class

**Assignment Operations:**

- `CREATE_ASSIGNMENT` - Create assignment
- `VIEW_ASSIGNMENTS` - Get assignments
- `SUBMIT_ASSIGNMENT` - Submit work
- `VIEW_SUBMISSIONS` - Get submissions

**Announcement Operations:**

- `POST_ANNOUNCEMENT` - Create announcement
- `VIEW_ANNOUNCEMENTS` - Get announcements
- `POST_COMMENT` - Add comment
- `VIEW_COMMENTS` - Get comments

**File Operations:**

- `START_FILE_TRANSFER` - Begin upload
- `FILE_CHUNK` - Send chunk
- `END_FILE_TRANSFER` - Complete upload
- `UPLOAD_MATERIAL` - Upload material
- `VIEW_MATERIALS` - Get materials
- `DOWNLOAD_FILE` - Download file

## Security Model

```
┌─────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Layer 1: Network Level                                 │
│  ┌──────────────────────────────────────────┐          │
│  │  TCP Connection                          │          │
│  │  - Reliable delivery                     │          │
│  │  - Connection-oriented                   │          │
│  └──────────────────────────────────────────┘          │
│                                                          │
│  Layer 2: Authentication                                │
│  ┌──────────────────────────────────────────┐          │
│  │  Password: SHA-256 hash                  │          │
│  │  Token: UUID4 session token              │          │
│  │  Validation: Every request               │          │
│  └──────────────────────────────────────────┘          │
│                                                          │
│  Layer 3: Authorization                                 │
│  ┌──────────────────────────────────────────┐          │
│  │  Role-based access control               │          │
│  │  - Teacher: Create, manage               │          │
│  │  - Student: View, submit                 │          │
│  └──────────────────────────────────────────┘          │
│                                                          │
│  Layer 4: Data Validation                               │
│  ┌──────────────────────────────────────────┐          │
│  │  File size limits: 50MB                  │          │
│  │  File type validation                    │          │
│  │  Input sanitization                      │          │
│  └──────────────────────────────────────────┘          │
│                                                          │
│  Layer 5: Email Security                                │
│  ┌──────────────────────────────────────────┐          │
│  │  SMTP with TLS encryption                │          │
│  │  App-specific passwords                  │          │
│  │  No plain text passwords                 │          │
│  └──────────────────────────────────────────┘          │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## Deployment Topology

### LAN Deployment

```
┌──────────────────────────────────────────────────────────┐
│              Local Network (192.168.1.0/24)              │
│                                                           │
│  ┌──────────────┐         ┌──────────────┐              │
│  │   Teacher    │         │   Student 1   │              │
│  │  Computer    │         │   Computer    │              │
│  │              │         │               │              │
│  │ 192.168.1.10 │         │ 192.168.1.11  │              │
│  └──────┬───────┘         └──────┬────────┘              │
│         │                        │                       │
│         └────────┬───────────────┘                       │
│                  │                                       │
│         ┌────────▼────────┐                              │
│         │                 │                              │
│         │  Server Host    │                              │
│         │  192.168.1.100  │                              │
│         │  Port: 8888     │                              │
│         │                 │                              │
│         │  - MongoDB      │                              │
│         │  - LearnLive    │                              │
│         └─────────────────┘                              │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### Internet Deployment (Future)

```
                    ┌─────────────┐
                    │   Internet  │
                    └──────┬──────┘
                           │
                  ┌────────▼────────┐
                  │  Cloud Server   │
                  │  (AWS/Azure)    │
                  │                 │
                  │  Public IP      │
                  │  Port: 8888     │
                  └─────────────────┘
```

---

**Key Points:**

- Multi-threaded TCP server
- Custom JSON protocol
- MongoDB for persistence
- ttkbootstrap for modern UI
- Email notifications via SMTP
- File transfer with chunking
- Token-based authentication
- Role-based access control

This architecture demonstrates production-ready networking concepts! 🎓
